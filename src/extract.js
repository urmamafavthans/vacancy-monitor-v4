/**
 * Phase 8 implementation point.
 * SOURCE route responsibility: produce candidate jobs from structured JobPosting,
 * ATS records, repeated vacancy cards, URL patterns, or strict inline vacancy sections.
 */
export function extractCandidates() {
  return [];
}
